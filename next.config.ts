import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "via.placeholder.com", "lh3.googleusercontent.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
